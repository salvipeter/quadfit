#pragma once

#include <geometry.hh>

Geometry::BSSurface connectG1(const Geometry::BSCurve &c,
                              const Geometry::BSCurve &c1,
                              const Geometry::BSCurve &c2);
